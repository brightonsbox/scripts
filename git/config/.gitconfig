[user]
	email = brightonsbox@hotmail.com
	name = Jon Brighton
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
[pull]
	rebase = false
[core]
    excludesfile = ~/.gitignore_global
[init]
    templateDir = ~/.git-templates/git-secrets
